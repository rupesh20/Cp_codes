#include <bits/stdc++.h>using namespace std;int main(){	int t;	string s[t],s;		for(int i=0;i<t;++i){			cin>>s;			s[i]=s		}		string t		for(int j=0;j<t;++j){			t=s[j];			int len=t.length();			for(int k=j+1;k<t;++k){				t1=s[k];				int len1=t1.length();				if(len==len1)				{					while(t[i]!='\0')					{						if(t[i]==t1[p])c++;					}				}				if(c==len)c=1;break;			}			if(c==1)break;		}		cout<<len<<endl;return 0;}